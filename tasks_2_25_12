taskTreeJS  + –
	Change expand/collapse to legit arrows (perhaps imgs?)  + –
	fix arrows  + –
	set default arrow visibility to none  + –
	Push changes to Git  + –
	delete arrow if parent node has no more children  + –
	freeze top div  + –
	opacity of lower 'layer'?  + –

><RL Plugin?  + –

↓allwebcafe application  + –
	><re-work resume?  + –
